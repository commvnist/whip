# Area deletion: readable preservation choices and cleanup meaning

Evidence for FND-20260909-011 / DEC-20260909-009 / IMP-20260909-010 / VER-20260909-010. Baseline: clean pushed `e62d72f`. All runtime work used one disposable API 34 emulator.

The ordinary dialog previously clipped the first destination below a long explanation, despite its passing callback test. The new complete-choice bounds assertion reproduced that failure. The corrected dialog shows nonzero counts with proper singular/plural wording, gives its existing list a larger parent-bounded cap and scrolling heading, and places preservation destinations before extended cleanup details. All actions retain their explicit meaning. Empty Areas now disclose saved filter/widget reset, matching existing unconditional cleanup behavior.

The ordinary before/after pairs use the same dark theme and fixture. Original final PNG bytes and original PNG/XML hashes were verified against `capture-manifest.tsv`; stored XML only normalizes CRLF to LF. All seven final images were personally inspected; [review.tsv](review.tsv) records their scoped conclusions. The enlarged fixture uses actual Android 200% text in a 320×600 dp window. Its complete error remains readable after scrolling, selection survives simulated failure/retry, and saving disables both actions and destination changes. Normal and enlarged neighbor choice lists retain their prior geometry.

Verification: the initial bounds regression failed 1/1 as expected. The first correction passed 2/3 because the new busy test wrongly expected both buttons to change their labels; fixing that assumption retained both disabled-action checks. All three focused tests then passed, followed by 73 adjacent Android regressions and a five-owner/seven-state exact capture, with zero failures/skips/reuse. The adjacent campaign includes repository-authoritative Area reassignment and mutation receipts, domain deletion coordination, Tag and backup choices, and shared/nested dialog behavior. Exact commands, run paths, readiness results, and limits are in VER-20260909-010.

This accepts the scoped Area-deletion layout and callback behavior. The new UI fixtures do not claim an end-to-end persisted UI deletion journey; existing repository/ViewModel regressions provide separate persistence evidence. Complete organization, platform, accessibility, and whole-product acceptance remain open. The Task keyboard/header and QA test-package routing findings remain separate follow-ups. No physical-device, version, release, or publication action occurred.
