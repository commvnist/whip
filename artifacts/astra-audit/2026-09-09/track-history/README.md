# Archived history: less guidance, earlier records

FND-20260909-017 / DEC-20260909-015 / VER-20260909-018. Baseline: clean `482669b` plus a nonvisual list tag and synthetic journey fixture. Whip remains unreleased 0.3.66/code 72; no schema or persisted-history changes.

The centered archive explanation becomes a compact “Archived · Read-only” status with a contextual Restore Track action. Short single-pane detail uses its existing Back route in place of a second fixed workspace tab row; collection and wide split views retain that row. Restoration uses the existing repository operation, and Entry mutation controls appear only after the archived flag changes durably.

Matched API 34 screenshots show the first Entry title moving from y=1422 to y=1201 at ordinary text and y=1873 to y=1429 at actual 200% text. Two complete initial records are now visible at enlarged text, replacing the partly visible first record.

## Evidence scope

- `before/`: two original API 34 opening PNG/XML pairs, ordinary and enlarged, from `1oomPx`.
- `final-api34/`: eight native states from the passing canonical run `JhiTrQ`: archived opening, browsing middle history, keyboard search and exact read-only inspector at both text scales. Every original image was individually inspected.
- `final-api26/`: eight replacement settled native captures on the 320×533 dp/API 26 emulator, from passing direct slot `astra-track-history-settled`. Every original image was individually inspected; the earlier inspector fade is absent.
- `final-api37/`: eight canonical wide-screen states from `3oPBCz`; every original image was individually inspected. Collection and detail remain side by side.
- `adjacent/`: four individually inspected final ordinary archived/active detail, Options and Insights frames from the existing owning catalog test, `Ox0yUm`. The full manifest contains its 18 outputs; the other 14 are outside this artifact's final personal-review scope.
- `tests/`, manifests and `review.tsv`: exact execution and per-image scope for all 30 retained original PNG/XML pairs. `scripts/check --ready` passes 344 JVM tests in 34 suites, Android compilation/debug packaging/lint/static gates; `ready-suites.tsv` preserves the routed JVM counts. Current source inventory is 630 JVM / 994 Android tests, 275 catalog states and 36 font fixtures. No full-suite or release claim follows these scoped checks.

Both real journeys seed 30 Entries, browse older records, search, recreate, open the exact read-only inspector, return to Archived, reopen, restore, recreate again and verify unchanged Field/Choice/Entry graphs plus restored Edit availability. The neighboring campaign passes 64 JVM and 97 Android tests in `HBV68I`, covering core Tracks, global routing, navigation, adaptive layouts and numeric Insights.

## Remaining review

FND-20260909-018 remains open. Enlarged inline search is partly covered by native keyboards, and API 26 pans identity under system bars. The short enlarged history viewport also needs further work so a record's identity and date are easier to read together. The API 26 system font lacks the fixture's boot emoji. Wide support-pane zero counts/repeated context and occasional tightly packed enlarged workspace labels remain broader design/discovery topics. These captures do not establish whole-Track or whole-product acceptance.

The first four-state adjacent capture ran its owner successfully but correctly failed artifact accounting because that owner emitted 18 states. The corrected capture inventory includes all 18. Early fixture failures and the legacy inspector transition capture are retained as history in VER-20260909-018; they are not final visual evidence.
