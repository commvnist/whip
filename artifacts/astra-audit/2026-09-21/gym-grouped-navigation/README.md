# Grouped-workout Next Set navigation — 2026-09-21

Scoped Gym audit evidence for FB-20260920-001 / FND-20260921-004, not whole-product acceptance or an owner-phone release. The data and screenshot come from a disposable API 34 emulator.

- The real-app fixture starts a two-exercise Superset. Bench has ten Sets before Row in the same rendered group. After completing Bench Set 1, group rotation correctly names Row Set 1 as NEXT. Before the fix, tapping the sticky NEXT row scrolled only to the group's first block; `active-set-composer` was not displayed (`build/instrumentation-results-LfGWbz`, 1/1 failed at the exact visibility assertion).
- The fix keeps the existing group-level lazy-list scroll to compose the target, then asks the active Set's own `BringIntoViewRequester` to expose that Set. The same exact target is used for automatic next-Set handoff, explicit NEXT taps and externally requested active-exercise navigation. No group order, prescription, completed Set, routine, Room schema, data epoch or backup format changes.
- The corrected journey enters through NEXT (also useful when large text initially puts the first composer below the lazy viewport), checks automatic target visibility, deliberately scrolls away and taps NEXT again, recreates the Activity, repeats the tap and verifies one completed Set. The accepted capture is after recreation and the explicit jump. The original [normal-phone PNG](gym.workout.grouped-next-set.png) and [XML](gym.workout.grouped-next-set.xml) were inspected at 1080×2520: the Row Set 1 identity, weight/repetition controls, Complete Set and Set Details are visible together below the sticky lane; XML includes `active-set-composer` and no `NAF` node.
- The same real journey passes at 1080×2400/420 dpi with ordinary text (`build/instrumentation-results-AiMGv7`), and at **actual Android font scale 2.0** (`build/instrumentation-results-vmiv3f`) after the correct NEXT-first test entry. Its [wide PNG/XML](gym.workout.grouped-next-set.wide.png) and [200% PNG/XML](gym.workout.grouped-next-set.large.png) originals are retained. At 200%, NEXT wraps but the target's weight, repetitions and Complete Set remain visible and the XML has no `NAF` node. The emulator font scale was restored to 1.0 afterward. The first enlarged-text attempt (`JBtftx`) failed before navigation because the test queried a lazy, not-yet-composed input; this is a fixture precondition correction, not a proven production defect.
- Final NEXT-first normal original SHA-256: PNG `d000dbaf3dd94ad6d9aa70dab61912b5774e94ced4c5863abdce69ae89dbe9be`; XML `b44e38295aae84e36778be6f2d8567da7f5545bc38c22c61e67970f29a7a696b`.
- Wide hashes: PNG `911a3b47ad572cfc0d3a144e59cb7c35c563b56d9233b0477dd310fa8aae022d`; XML `80a1a63135362eff03659476d942ee288bbe2baab92d9fc1f472d43df7d49a13`. 200% hashes: PNG `db8982da9628587760536d2a57b1df738ca661fa1a2cb69753d9b1cd3cfed523`; XML `5d333a4d9392e8bc4348c980d40fbaaaacfc3a95225d1242fcb456aab31a4d9b`.
- Focused corrected runs: `build/instrumentation-results-Pc9f2z` before the entry-path refinement and final `build/instrumentation-results-Mi9Xlw`, both 1/1 passed, zero skipped. The affected Gym/Gym-phased profile passes all 174 Android methods across two fresh batches in `build/instrumentation-results-qB2pgJ` with zero failures/skips/reuse. `scripts/check --ready` passes routed JVM, harness fixtures, Android compilation, lint and debug packaging in 2m11s. A current Gym-family capture passes all 36 owners/87 states with no missing/extra captures, duplicate pixels or NAF nodes in `build/astra-gym-grouped-final-20260921/`; its normal phased structure, primary progression, active phased workout and Machine editor originals were personally reviewed. The final NEXT-first test-source refinement is separately replayed on both device sizes; the full final-source product matrix, exact whole catalog and remaining platform/advanced Gym configurations remain open.
