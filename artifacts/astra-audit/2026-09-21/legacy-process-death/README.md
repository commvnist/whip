# Active legacy workout through real Android process death

Scope: FB-20260920-001/002 and IMP/VER-20260921-008. This closes the true OS-process-death gap left by the Activity-recreation journey in IMP/VER-20260921-006. It is a disposable API 34 emulator fixture, not an owner-phone test or a new 5/3/1 authoring flow.

## Replay

`replay.py` refuses non-emulator targets, seeds one active legacy workout through the production repositories using an explicit instrumentation flag, opens its actual finish review through the app shell, sends the Activity to Home, brings Android Settings forward, and asks ActivityManager to kill the cached Whip process. It verifies the old PID is absent before refocusing the retained task and requires a different new PID. This uses Android's managed background kill; an initial raw `SIGKILL` harness attempt was rejected because Android treated it as a crash and removed the task, while an immediate `am kill` with Whip still the previous process was a no-op. Neither was accepted as product evidence.

The accepted `run-api34/proof.json` records task 951, old PID 30682, new PID 30904, and exact Room facts. Before and after process death, the reviewed Routine remains `FiveThreeOne` while its active session remains in review with the same selected Standard increase. Applying the decision leaves session 7 `Finished|FiveThreeOne` with progression advanced and its performed Set still `TrainingMaxTest`; only saved Routine 6 changes to `Custom`. The next Routine card says `Phased Routine`, and starting it creates session 8 `Active|Custom`. This proves no historical performed-set rewrite or reentry into branded authoring.

Five exact-source original 1080×2400 PNG/XML pairs are retained in `run-api34`: before review, recovered review, finished empty Workout, generic next-Routine card, and next active generic workout. The before/recovered frames are intentionally pixel-identical because the same review and choice were restored. They were individually inspected: the review action/selected increase is readable and unclipped, the post-finish Workout returns to an empty state, and generic Routine identity/next action remain legible. The XML pairs have no `NAF=true` nodes. `background.txt`, seed/launch logs and the proof receipt retain the task/PID/durable-state trace without app-private database content.

The ordinary `GymLegacyRetirementJourneyE2ETest` continues to run without the opt-in flag and cleans its synthetic records; its final exact rerun passes 1/1 in `build/instrumentation-results-vhOJe5`. The seed path confirms Setup settings durably before its instrumentation process exits. All 659 JVM methods, the 27-second final readiness/lint/build gate, 528-state catalog lint and `git diff --check` pass. No production source, Room schema, backup format, data epoch, package version or owner-phone state changed. More advanced Gym configurations and full current-catalog/product acceptance remain open.
