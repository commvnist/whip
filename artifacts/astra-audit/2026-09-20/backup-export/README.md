# Native backup-export interruption evidence — 2026-09-20

This is a scoped Data & Privacy checkpoint under FND/DEC-20260920-002, not completion of the backup or whole-product audit. All files and records are synthetic on a disposable API 34 emulator; no owner phone or private backup was accessed.

- Baseline `build/instrumentation-results-X39bS4`: the real Android DocumentsUI picker remains open while Whip's Activity is recreated. After selecting Downloads and saving the encrypted destination, the old composition-owned passphrase is gone: expected `Backup saved`, actual `Enter an encryption passphrase`. An earlier `ActivityScenario.recreate()` attempt was a fixture failure because Whip was STOPPED beneath the picker; `Activity.recreate()` from the main thread is the accepted interruption procedure.
- Final source `build/instrumentation-results-nlwRGA`: all seven `DataPrivacyJourneyE2ETest` methods pass. The encrypted file saved after the same interruption decrypts with the authored passphrase and contains the exact seeded Task. Native plain JSON and Tasks CSV exports retain their requested formats and record content. Cancellation/lost-request behavior fails closed with a visible retry explanation; native restore, checksum rejection, wrong-passphrase retry, Merge and dark/light journeys remain green.
- Neighbor run `build/instrumentation-results-fglCoQ`: 15 accepted Android methods across the lost-request replacement, Settings behavior and encrypted codec checks pass. The final-source lost-request copy change is included in that run; the seven-method final class run supersedes all earlier class outcomes.
- Three original PNG/XML pairs here were captured from the exact-source class run. I inspected each at 1080×2520/480 dpi: encrypted, plain and CSV receipts explicitly name the resulting format, keep one clear Dismiss action and preserve the Data & Privacy context. XML has no NAF nodes. The current catalog has 523 required captures, zero pending selectors or platform exceptions.
- Final `scripts/check --ready` passes in 2m30s with 656 JVM methods, Android-test compilation, lint and debug packaging; it creates no frozen store-candidate evidence.

Portable-folder provider failure/revoked access, process recovery, full replacement/reset failure injection, complete Settings review, full Android matrix and full current-catalog recapture remain open under FB-20260920-001.
