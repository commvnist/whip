# Portable-folder recovery evidence — 2026-09-20

This is a scoped provider-access checkpoint under FND/DEC/IMP/VER-20260920-004, not completion of backup or the whole-product audit. All records and external files are synthetic on a disposable API 34 emulator.

- Fake-provider baseline `build/instrumentation-results-LnSWTR` executes 15 manager methods and fails only the two new recovery assertions: a thrown listing failure leaves no `lastError`, and a provider's `delete(false)` response is treated as successful cleanup. The other 13 methods pass.
- Production keeps startup non-fatal but makes the manager own a durable recovery receipt. Listing errors and refused exact-owned partial deletion become visible without changing folder configuration, automatic policy, app records or the last verified timestamp/name. A successful new backup can coexist with a cleanup warning. Reselecting a folder clears it; forgetting a stale grant remains reliable.
- Platform permission failures now read `Whip no longer has access to the selected backup folder`; the underlying exception remains the cause for diagnostics. The rejected before PNG/XML shows why this matters: Android's raw provider class, `ProcessRecord`, PID and UID filled the card. The final pair is concise, contains the prior verified receipt and exposes `Back Up Now`, `Reconnect or Change Folder`, and `Forget Folder` without NAF nodes.
- `build/instrumentation-results-H53M0E` proves that releasing an already-gone old grant used to fail after a replacement folder was committed. Exact correction `build/instrumentation-results-8b5cin` passes, and `build/instrumentation-results-0t5MPQ` passes all 19 final manager methods, including permission loss during selection and stale-old-grant replacement. `build/instrumentation-results-wYbG9g` passes all 8 Data & Privacy methods after sanitization, and final-source `build/instrumentation-results-KBDZab` replays the exact real SAF recovery method after the selection-time correction: DocumentsUI grants a dedicated writable subfolder; Whip writes and verifies a backup; the test releases the persisted grant and temporarily moves the real tree out of provider reach; recovery preserves records/receipt and warns; restoring and reselecting the folder clears the warning and another verified backup succeeds. Earlier checkpoints remain diagnostic history, not final-source acceptance.
- Early native fixture failures selected Android's intentionally prohibited storage/Downloads roots, assumed one picker drawer state, or treated release of a persisted grant as immediate loss while the live instrumentation process still held a temporary grant. They are excluded rather than mislabeled as product failures.
- All 657 JVM methods pass. `scripts/ui-catalog lint` reports 524 required states, zero pending/exception. Final post-change `scripts/check --ready` passes in 2m7s with JVM execution, Android-test compilation, lint and debug packaging; no frozen candidate is created.

Third-party Drive/removable-media outages, a true process-kill grant-revocation campaign, replacement/reset failure injection, the complete Android matrix and full 524-state recapture remain open under FB-20260920-001.
