# Current visual-alias disposition — 2026-09-21

The [September 8 review matrix](astra-surface-review-2026-09-08.tsv) remains immutable historical evidence. The fresh current-source 528-pair attempt executed 193/193 selectors and exported every pair, but its strict duplicate-pixel gate correctly refused acceptance. Five pairs were byte-identical; a sixth had identical Whip content and differed only by the Android clock changing from 5:28 to 5:29. These are behavioral transition endpoints, not six additional visual surfaces. [FND-20260921-008](../product-memory/FINDINGS.md) and [DEC-20260921-008](../product-memory/DECISIONS.md) retain the distinction.

| Historical/current alias removed from current catalog | Canonical current visual card | Behavior still asserted in its native journey |
| --- | --- | --- |
| `settings.delete-retry.large-message` | `settings.delete-retry.large` | `SettingsResponsiveUiTest#destructiveCopyAndActionsRemainReachableInAShortLargeTextWindow` scrolls the error into view, checks its full bounds inside the reading viewport, then checks fixed Cancel/Retry actions. The full error was already visible in the earlier capture. |
| `shared.review.empty-selection-reopened` | `shared.review.empty-selection` | `ReviewJourneyE2ETest#changingReviewOptionsRevealsSavedOutcomesWithoutLosingTheView` includes Tasks, recreates the Activity, returns to empty Habits, recreates, closes/reopens Review and checks empty copy again. The final visual is the initial collapsed empty view. |
| `tracks.collection.unpin.ordinary` | `tracks.collection.visible-selection.ordinary` | `TrackCollectionJourneyE2ETest#areaChangesKeepOnlyVisibleSelection` checks narrowed Area/selection and enabled, displayed Unpin before mutation; the action is already visible in the narrowed-selection card. |
| `tracks.collection.unpin.large` | `tracks.collection.visible-selection.large` | The same `#areaChangesKeepOnlyVisibleSelectionAtLargeText` assertion and 200% text check; scrolling to Unpin does not move the already-visible action. |
| `tracks.csv-recovery.replacement-cancelled.ordinary` | `tracks.csv-recovery.invalid.ordinary` | `TrackCsvJourneyE2ETest#invalidFileCanBeReplacedWithoutImportingPartialRows` invokes the real Android picker and presses Back; the invalid row and disabled import remain asserted, with no partial rows. The returned review is byte-identical to the initial invalid review. |
| `tracks.csv-recovery.replacement-cancelled.large` | `tracks.csv-recovery.invalid.large` | The 200% variant repeats the real picker cancellation and invalid/disabled checks. Source XML differs only in `com.android.systemui:id/clock`; no Whip content changes. |

The historical verification claims are not revoked or rewritten. The alias consolidation initially left 522 distinct capture IDs; the separately justified scrolled `settings.delete-retry.large-impacts` card brings the current catalog to 523. It shows backup/consequence copy that the error-only alias never showed. The six aliases are explicitly mapped, and their journey assertions remain. The previous full-capture directory `build/astra-full-current-20260921-v2` is diagnostic evidence, **not accepted**. Fresh complete current-source capture and personal review are still required before whole-product visual acceptance.
