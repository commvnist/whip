# Frozen 5/3/1 surface disposition — 2026-09-21

The [September 8 review matrix](astra-surface-review-2026-09-08.tsv) is immutable historical evidence. This is the current-source disposition of its 18 branded Gym rows after FB-20260920-002; it does not close the other unfinished Gym rows or the whole-product audit.

| Frozen surface IDs | Current disposition | Evidence and limit |
| --- | --- | --- |
| `gym.531.setup.large`, `gym.531.setup`, `gym.531.setup-blocked` | Retired: no new 5/3/1 setup or empty-library invitation. | The setup generator/wizard was removed in `74f31589`. Ordinary Routine outline and `gym.routine-builder.phases` now author phases directly; `RoutineBuilderUiTest#ordinaryRoutineCanAddIndependentPhasesWithoutFiveThreeOneSetup` captures and checks this path. Its three [originals](../../artifacts/astra-audit/2026-09-20/gym-retirement/README.md) are scoped, not the final full catalog. |
| `gym.531.workout.active` | Replaced for new authoring by `gym.routine-phased.workout`. | `GymPhasedRoutineJourneyE2ETest#routineStartsFromLibraryAndShowsItsActualPhaseInWorkout` starts an ordinary phased Routine and checks actual phase, prescription and NEXT. Historical workout snapshots still retain their true original provenance; the old branded state is not a new-workout destination. |
| `gym.531.cycle-review.large`, `gym.531.review` | Legacy-active-workout-only: current catalog IDs `gym.legacy-program.review.large` and `gym.legacy-program.review`. | `GymScreens` opens `FiveThreeOneCycleReviewDialog` only when `activeFiveThreeOneCycleReview()` returns a review for an already-active legacy session. Its dialog controls remain in `FiveThreeOneCycleReviewUiTest`, including 200% text. A real process-interrupted old-session-to-review journey is still open; these IDs are not described as a normal new feature. |
| `gym.531-authorship.reordered`, `gym.531-authorship.library-return` | Retired specialized authoring. | Generic Routine exercise picking, reordering, Training Max and percentage prescription are retained under `RoutineBuilderUiTest`, `RoutineAuthoringJourneyE2ETest` and the phased states. The old branded setup cannot be initiated. Per-exercise BBB/FSL configuration is deliberately *not* a generic default under DEC-20260920-001. |
| `gym.531-authorship.workout`, `gym.531-cycle.setup`, `gym.531-cycle.workout`, `gym.531-cycle.focused-set`, `gym.531-cycle.cycle-complete`, `gym.531-cycle.review`, `gym.531-cycle.decision`, `gym.531-cycle.next-cycle` | Retired 5/3/1-specific generation and cycle protocol; generic phased execution/progression replaces the reusable portion. | `GymPhasedRoutineJourneyE2ETest`, `RoutineRepositoryTest` and the two ordinary phased catalog states verify authored phase, Training Max boundary and actual workout progression. An already-active legacy session can finish through its legacy review, but no new 5/3/1 cycle is generated. `gym.workout.grouped-next-set` separately addresses generic exact Set navigation; it is not a rebranded cycle protocol. Complete advanced-configuration campaign remains open. |
| `gym.531-authorship.history`, `gym.531-cycle.history` | Retired dedicated capture route, **retained performed history**. | `LegacyRoutineRetirement.kt` updates only future templates inside a Room transaction. The conversion test `RoutineRepositoryTest#legacyRoutineConversionWaitsForActiveWorkoutAndKeepsCompletedSnapshot` checks active-session gate, completed snapshot and idempotence. `gym.history.populated`/`gym.history.expanded` own general historical presentation; final-source full visual and real-device review remains open. |

Additional post-freeze branded catalog states removed by `74f31589`: `gym.531-supplemental.bench-setup`, `.mixed-preview`, `.bench-workout`, `.zercher-workout`, and `gym.531-edit.supplemental-scope`, `.saved-supplemental`, `.updated-workout`. Their BBB/FSL-specific authoring is retired; ordinary per-exercise phased prescriptions remain. Two old review IDs were reclassified to legacy-only states as above. The old `gym.routine-builder.program` page is superseded by `gym.routine-builder.phases` and `.primary-progression`, while `gym.routine-builder.outline` and `gym.routine.active-blocked` still exist with current ordinary-Routine owners.

Compatibility boundary: `WhipApplication` converts inactive legacy templates on startup and after the source active workout exits; `LegacyRoutineRetirement.kt` preserves routine/day/exercise/Set identities and completed sessions, clears future scheme/Joker metadata and pauses a review-only progression boundary rather than silently enabling an automatic increase. This current-source disposition follows [DEC-20260920-001](../product-memory/DECISIONS.md); it is not a claim that all Gym states, API variants or legacy interrupted-session flows have final acceptance.
